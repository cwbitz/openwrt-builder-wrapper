# Main Router Configuration

## Overview

This module configures the LAN interface static IP address for OpenWrt operating as the main router.

## Parameters

### Environment Variables
- `BW_MAIN_LAN_IP` — LAN interface static IP address (default: `192.168.2.1`)

## Use Cases

For main router deployments where only the LAN subnet IP configuration needs to be modified.
